# Litmus project configuration
name: demo
default_station: demo_station_001
mock_instruments: true
results_dir: results

reports:
  auto: false
  format: html
  template: default
  output_dir: reports
