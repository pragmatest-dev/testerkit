"""Storage backends for test results.

Import directly from the submodule that owns the type:

    from testerkit.data.backends.parquet import ParquetBackend, materialize_run_to_parquet
    from testerkit.data.backends.protocol import MeasurementWriter
"""
