[pytest]
# Tier 3 example — PMIC-A23 pytest-native flow exercising all Litmus seams.
# Run from repo root:
#   cd examples/03-profiles && uv run pytest --mock-instruments -v
addopts = --operator=demo_operator --dut-part-number=PMIC-A23 --dut-serial=PMIC-A23-001
testpaths = tests
