# Family base — not directly selectable (no facets).
# Scenario files extend this to inherit its markers and override what
# they need. Kept narrow: everything true for every rail test, regardless
# of phase.
description: "Shared base for rail scenarios"
tests:
  test_rail_holds_across_input:
    config:
      - litmus_vectors:
          - {vin: [3.3, 5.0, 5.5]}
