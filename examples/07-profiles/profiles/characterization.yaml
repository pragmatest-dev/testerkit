# Characterization — wide sweep, no limits (record-only).
facets:
  test_phase: characterization
tests:
  test_rail_holds_across_input:
    sweeps:
      - {vin: [3.0, 3.3, 3.6, 4.0, 4.5, 5.0, 5.5, 6.0]}
