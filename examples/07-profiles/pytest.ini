[pytest]
# --mock-instruments default-on so the demo runs without hardware.
# Profiles bind station_type + fixture per phase; the underlying
# `mock: true` station entries still apply. Pass --test-phase=<id>
# (development / production / characterization) to select a profile.
addopts = --mock-instruments
