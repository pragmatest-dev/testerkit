id: bench
description: |
  Minimal bench station-type — a PSU to source the UUT input and a DMM
  to measure the UUT output. Concrete bench instances declare which
  resources (GPIB, TCP/IP, etc.) play each role.

instruments:
  psu:
    type: psu
    driver: drivers.PSU
  dmm:
    type: dmm
    driver: drivers.DMM
