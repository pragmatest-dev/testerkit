[pytest]
# Vanilla stage: tests are pure pytest, but the Litmus plugin stays
# enabled so you get the session-level value prop on day one — DUT
# serial prompt + run record — without writing any Litmus-specific
# code. Pass --dut-serial=<value> to skip the prompt; later stages
# layer measurement-level features (verify, limits, markers) on top.
