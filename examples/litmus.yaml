# Litmus project configuration
name: demo
default_station: demo_station_001
default_fixture: power_board_fixture
mock_instruments: true

# Pluggable outputs (uncomment to enable)
# outputs:
#   - format: html
#   - format: csv
