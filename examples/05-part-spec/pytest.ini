[pytest]
# --mock-instruments default-on so the demo runs without hardware.
# The part spec stage layers spec-driven limits on top of the
# 04-sidecar-markers setup — same mock fixtures, same `psu`/`dmm`
# driver wrappers, drop the flag (or pass --no-mock-instruments)
# once a bench is attached.
addopts = --mock-instruments
