[pytest]
# Mock the scope + PSU by default — runs without a bench. The Scope mock's
# capture() returns a freshly synthesized step-response waveform each call.
# Drop this flag or pass --no-mock-instruments to point at a real bench.
addopts = --mock-instruments
